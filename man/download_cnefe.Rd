% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/download_cnefe.R
\name{download_cnefe}
\alias{download_cnefe}
\title{Download the geocodebr version of the CNEFE data set}
\usage{
download_cnefe(abbrev_state = NULL, showProgress = TRUE, cache = TRUE)
}
\arguments{
\item{abbrev_state}{description year}

\item{showProgress}{Logical. Defaults to \code{TRUE} display download progress
bar. The progress bar only reflects only the downloading
time, not the time to load the data to memory.}

\item{cache}{Logical. Whether the function should read the data cached
locally, which is much faster. Defaults to \code{TRUE}. The first time the
user runs the function, \code{censobr} will download the file and store it
locally so that the file only needs to be download once. If \code{FALSE},
the function will download the data again and overwrite the local file.}
}
\value{
A directory path where the data was saved.
}
\description{
Downloads an enriched version of the CNEFE data set that has been
purposefully built to be used in the geocodebr package.
}
\examples{
\dontshow{if (identical(tolower(Sys.getenv("NOT_CRAN")), "true")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# download CNEFE for a single state
download_cnefe(abbrev_state = "AC",
               showProgress = FALSE)

# download CNEFE for multiple states
download_cnefe(abbrev_state = c("AC", "AL"),
               showProgress = FALSE)

# download CNEFE for all states
download_cnefe(abbrev_state = "all",
               showProgress = FALSE)
\dontshow{\}) # examplesIf}
}
\concept{Support}
