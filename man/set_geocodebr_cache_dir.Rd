% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/set_geocodebr_cache_dir.R
\name{set_geocodebr_cache_dir}
\alias{set_geocodebr_cache_dir}
\title{Set custom cache directory for geocodebr files}
\usage{
set_geocodebr_cache_dir(path = NULL)
}
\arguments{
\item{path}{String. The path to an existing directory. It defaults to \code{path = NULL},
to use the default directory}
}
\value{
A message indicating the directory where geocodebr files are cached.
}
\description{
Set custom directory for caching files from the geocodebr package. If users
want to set a custom cache directory, the function needs to be run again in
each new R session.
}
\examples{
\dontshow{if (identical(tolower(Sys.getenv("NOT_CRAN")), "true")) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
# Set custom cache directory
tempd <- tempdir()
set_geocodebr_cache_dir(path = tempd)

# back to default path
set_geocodebr_cache_dir(path = NULL)
\dontshow{\}) # examplesIf}
}
\seealso{
Other Cache data: 
\code{\link{geocodebr_cache}()}
}
\concept{Cache data}
